#!/bin/bash

alias c="clear"
alias ls="ls --color"
alias ll='ls -alF'
alias la='ls -Al'
alias l='ls -CF'
alias cours='cd /home/kupi/school/second-year'
alias gc='function _gc(){ git add . && git commit -m "$*" && git push; }; _gc'
alias nf=neofetch
alias dot="cd ~/.dotfiles"

zshrc() {
  "$EDITOR" "$HOME/.zshrc"
}
